<%= simple_form_for(@line) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :number %>
    <%= f.input :name %>
    <%= f.association :dispense %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
